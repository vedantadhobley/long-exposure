# Launch Dataset Status — 2026-05-31

Final state of the v16 launch dataset after Saturday's wrap-up day + overnight Phase E + Sunday-morning fix workflow.

## TL;DR

**Dataset is launch-ready.** 11 days + 3 weeks at 100% verifier-passed across all tiers. All work behind the unified `PipelineWorkflow` entry point. Frontend can start integration today.

## Per-day state

| Trading date | Narratives | Pass rate | Synthesis (v12) | Notes |
|---|---:|---:|:---:|---|
| 2026-05-08 | 194 | 100.00% | ✅ | Re-scored Sun morning to fix data freshness (was 91.46% under v14b breakdowns) |
| 2026-05-11 | 195 | 100.00% | ✅ | |
| 2026-05-12 | 158 | 100.00% | ✅ | |
| 2026-05-13 | 180 | 100.00% | ✅ | |
| 2026-05-14 | 210 | 100.00% | ✅ | |
| 2026-05-15 | 215 | 100.00% | ✅ | |
| 2026-05-18 | 218 | 100.00% | ✅ | |
| 2026-05-19 | 220 | 100.00% | ✅ | |
| 2026-05-20 | 226 | 100.00% | ✅ | |
| 2026-05-21 | 214 | 100.00% | ✅ | |
| 2026-05-22 | 207 | 100.00% | ✅ | |
| **Total** | **2,437** | **100.00%** | | |

## Weekly aggregates

| Week start | Days | Aggregate (v10) | Notes |
|---|---|:---:|---|
| 2026-05-04 | 05-08 only (single-day week) | ✅ | Cascade-fired from 05-08 |
| 2026-05-11 | 05-11..05-15 | ✅ | Cleared on retry after Phase E IMTX/NOK fab |
| 2026-05-18 | 05-18..05-22 | ✅ | Cleared on retry after streak-bound fab |

## Prompt versions in production

| Tier | Prompt | Last bump |
|---|---|---|
| DESCRIBE extract | `extract-v16-no-literal-day-count-2026-05-30` | Phase E pre-cycle |
| DESCRIBE render | `render-v11-co-occurring-maxlength-2026-05-30` | Round 6 |
| INTERPRET | `interpret-v14-no-literal-day-count-2026-05-30` | Phase E pre-cycle |
| SYNTHESIZE | `synthesize-v12-no-literal-day-count-2026-05-30` | Phase E pre-cycle |
| AGGREGATE-week | `aggregate-v10-no-literal-day-count-2026-05-30` | Phase E pre-cycle |
| AGGREGATE-quarter | `aggregate-quarter-v4` | Pre-dormant |
| AGGREGATE-year | `aggregate-year-v4` | Pre-dormant |

## Data table state

All 11 daily syntheses + all 3 weekly aggregates have `data_table` JSONB populated via Sunday-morning `DataTableBackfill` sweep with the latest builder code. Schema:

- Daily `data_table`: `executive_summary` (5 bullets), `headline` (top 5 events), `per_scorer_top` (top 5 per scorer with scorer-specific column projection), `day_summary` (counts, top symbols, HHI, entropy), `notable_extremes`, `vs_prior_day` (where applicable).
- Weekly `data_table`: `executive_summary` (5 bullets), `headline_events` (top 10), `per_day` breakdown, `top_symbols` with `days_present` + `scorer_mix`, `scorer_mix`, `notable_extremes`, `vs_prior_week`.

## Production-path validation

Phase E was the first end-to-end run of `PipelineWorkflow` as the unified operator entry. Result:

- 11 days × full LLM chain in 11h 41m via `Mode=LLM_CHAIN, cascadeRollups=true`
- Cascade fired weekly (3), quarterly (dormant), yearly (dormant) — all completed cleanly
- Schedule was switched from legacy `DailyPipelineWorkflow` to `PipelineWorkflow` mid-workflow — registration verified via decoded base64 args
- **Operational rule established**: `PipelineWorkflow` is THE operator-facing entry point. Legacy `DailyPipelineWorkflow` + leaf child workflows are INTERNAL composition only.

## Failures caught + fixed during the cycle

These are documented because they're load-bearing for future operational thinking:

1. **`SchemaManager.apply()` once-per-JVM guard** (`faeb10c`) — concurrent activities serialized at Postgres `pg_type` ACCESS EXCLUSIVE lock, dropping pace from 3.3/min → 1.5/min. Schema-guard restored Phase A/B sliding-window pace.

2. **`v15→v16` day-count fabrication** (`c9cad47`) — my canonical vocabulary example used "the trailing 14-day median" as a literal phrase. Model treated "14" as canonical and wrote it regardless of actual `baseline_window_trading_days` (which is 9-10 for most inter-day events). 9 events failed v13 verifier on Phase D2 audit. Fix: drop literal day counts entirely, use "trailing 2-week median" universally. **Pattern lesson**: never use a literal value as canonical-vocab example IF that value varies per-event.

3. **Non-ASCII char check** (`e9c4248` + `cf909a9`) — Qwen3.5 occasionally emits non-Latin tokens under RENDER preset (observed: SPY narrative ended with `9 price水平和` Chinese fragment). `ProseCharCheck` added as Layer 0 fail-fast across all 3 verifiers. Followup commit allows `µ`/`μ` (microseconds) since `BreakdownFmt` pre-formats sub-millisecond lifetimes with Greek mu.

4. **Tie-aware bullet rendering** (`c17d8ca`) — replaced margin-guard hide pattern with tie-aware expose pattern. "IWM, SPY, TQQQ, and NCI each led with 8 events" beats picking one arbitrary leader. User feedback caught this.

5. **SQL tie-break determinism** (`c17d8ca`) — `ORDER BY COUNT(*) DESC` had non-deterministic results on tied days. Added `, symbol ASC` / `, scorer_id ASC` secondary sort.

6. **05-08 data freshness** — Phase E ran `Mode=LLM_CHAIN` against pre-Round-6 breakdowns; the v16 prompt expected `halt_phase_span_label` / `duration_humanized` / `burstiness_class` fields that didn't exist. Sunday-morning fix workflow re-scored 05-08 with current code. **Operational pattern**: when re-running under new prompts, ensure breakdowns are fresh via SCORE_AND_LLM, not LLM_CHAIN.

7. **Weekly fabrication patterns** — week-of-05-11 (IMTX ticker + "NOK 14 sweeps" count) + week-of-05-18 ("fifth consecutive week" streak claim with only 2 prior weeks). Both caught by AttributionVerifier / streak-bound checks; both cleared on additional retry. The verifier moat is doing real work.

## Round 9 polish items deferred post-launch

1. **Iceberg DESCRIBE "display ratio" leak** — per-scorer extract prompt for iceberg renders "display ratio of N%" instead of the canonical "displayed tip represented N% of total executed". INTERPRET tier already renders canonical correctly on the same events. Fix: tighten iceberg-specific per-scorer extract prompt.

2. **Shortened source_field path model behavior** — model writes `post_cancel_cluster.count` instead of `co_occurring.during_event.post_cancel_cluster.count`. Verifier correctly rejects. 3 retries don't clear because model has strong prior. Options: (a) tighten prompt with explicit "full dotted path required" rule, (b) loosen verifier to accept leaf-key match when unique, (c) post-process blueprint to expand paths in `BlueprintExtractor`. Affects 6/179 events on 05-20.

3. **Cross-symbol context expansion** — sector + correlated symbols. Documented in `docs/todo.md` as Round 8 candidate. Largest single piece of post-launch work (~16-20 hr). Whitepaper-flagged gap.

## What you can hit Sunday with

- **Frontend integration in `vedanta-systems`** — read-only consumption against finalized v16 dataset
- **Whitepaper** — clean dataset to reference for examples + numbers
- **Cron schedule unpause** — when ready for autonomous nightly cron, `temporal schedule toggle --schedule-id daily-pipeline-cron --unpause`. Schedule is registered with `PipelineWorkflow` + `PipelineInput{mode=FULL_PIPELINE, cascadeRollups=true, pollUntilReady=true, runRetentionSweep=true}`.

## API surface for frontend

These endpoints need to be wired in `vedanta-systems/src/server/routes/long-exposure.ts`:

```
GET /api/long-exposure/health
GET /api/long-exposure/latest                  # latest trading_date with synthesis
GET /api/long-exposure/dates                   # all available trading_dates
GET /api/long-exposure/day/{YYYY-MM-DD}        # synthesis + data_table + grouped events
GET /api/long-exposure/week/{YYYY-MM-DD}       # weekly aggregate + data_table + per-day
GET /api/long-exposure/event/{id}              # full event drill-down (DESCRIBE + INTERPRET)
GET /api/long-exposure/symbol/{symbol}         # events for a ticker across days (optional v1)
```

The `day` endpoint should return the full data_table in one round-trip so the landing page renders without waterfall. Event drill-down lazy-loads on click.

## Recap of today's commits

```
c17d8ca fix(synth): tie-aware exec-summary bullets + deterministic SQL ordering
e9c4248 feat(verifier): non-ASCII char check across all 3 verifiers
cf909a9 fix(verifier): allow µ/μ (micro sign) in ASCII check
c9cad47 fix(prompts): drop literal day-count from canonical baseline vocab
af14231 docs(agents): Round 8 — wrap-up day capturing today's work
```

Plus the earlier Saturday foundation:
- `faeb10c` schema-guard
- `b2ec57b` verifier robustness
- `6e40bbb` data tables (Round 7)
- `df6bb09` DataTableBackfill CLI
- `07cafff` daily exec summary + cap 5
- `c947df2` vs_prior_period bullets
- `7c71d1e` canonical vocabulary (4 tiers)
